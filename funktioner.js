// Funktioner 

//Opgave 1

function Multiply(x, y) {
    if (x, y) {
        return 0;
    } else {
        return x * y;
    }
}

console.log(Multiply(10, 19));

// Opgave 2 

function Multiply(x, y) {
    if (isNaN(x) || isNaN(y)) {
        return 0;
    } else {
        return x * y;
    }
}

console.log(Multiply(14, 11));

// Opgave 3

function Multiply(x, y) {
    if (isNaN(x) && isNaN(y)) {
        return 0;
    } else {
        return x * y;
    }
}

console.log(Multiply(15, 17));


